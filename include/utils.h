#ifndef UTILS_H
#define UTILS_H

// Helper functions (string, path, error utils) declarations

/**
 * Trims leading and trailing whitespace from a string.
 * @param str The string to trim.
 * @return Pointer to the trimmed string.
 */
char *trim_whitespace(char *str);

#endif // UTILS_H
